scalar DateTime

type Agent {
  id: ID!
  name: String!
  hostname: String!
  os: String!
  status: AgentStatus!
  lastSeen: DateTime
  createdAt: DateTime!
}

enum AgentStatus {
  ONLINE
  OFFLINE
}

type Query {
  agents: [Agent!]!
  agent(id: ID!): Agent
}
